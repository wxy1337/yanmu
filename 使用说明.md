# 言幕使用说明

言幕会自动检测视频中的主要口语：

- 检测为中文（含普通话、粤语）时，统一繁转简并生成纯简体中文字幕；
- 检测为其他语言时，第一行保留外文原文，第二行添加简体中文翻译；
- 默认导出 `.srt` 字幕；勾选“压制到视频”后，会额外导出带硬字幕的 `.mp4` 成片。

## 1. 运行环境

### 推荐配置

| 项目 | 最低要求 | 推荐 |
| --- | --- | --- |
| 系统 | Windows 10、macOS 或 Linux | Windows 11 / Linux |
| Python | 3.10 | 3.11 或 3.12 |
| 内存 | 8 GB | 16 GB 以上 |
| 磁盘 | 5 GB 可用空间 | 10 GB 以上 |
| 视频工具 | 项目自带 FFmpeg，PyAV 代替缺失的 FFprobe | 系统 FFmpeg 最新稳定版 |

有 NVIDIA 显卡时，`faster-whisper` 可以使用 GPU；没有显卡也可在 CPU 上运行，只是耗时更长。

## 2. Windows 安装与启动

### 2.1 安装 Python 和 FFmpeg

安装 Python 3.10 或更高版本，并勾选“Add Python to PATH”。项目依赖已包含可用的 FFmpeg；如需使用系统版本，可在 PowerShell 中安装：

```powershell
winget install Gyan.FFmpeg
```

安装系统 FFmpeg 后重新打开 PowerShell，可确认命令是否可用：

```powershell
python --version
ffmpeg -version
ffprobe -version
```

### 2.2 安装项目依赖

在项目目录 `E:\Translate` 中运行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
.\setup.ps1
```

脚本会创建 `.venv` 虚拟环境、安装依赖，并从 `.env.example` 复制出 `.env` 配置文件。

### 2.3 启动服务

```powershell
.\start.ps1
```

看到 Uvicorn 启动信息后，访问：

<http://localhost:8000>

停止服务时在 PowerShell 窗口按 `Ctrl+C`。

## 3. Docker 启动

电脑已安装 Docker Desktop 时，可以跳过本机 Python 和 FFmpeg 安装：

```powershell
Copy-Item .env.example .env
# 编辑 .env，按第 4 节配置翻译服务
docker compose up --build
```

然后访问 <http://localhost:8000>。后台运行可使用：

```powershell
docker compose up -d --build
```

停止容器：

```powershell
docker compose down
```

视频任务保存在本地 `data` 目录，Whisper 模型保存在 Docker 命名卷 `whisper-models`，重建容器不会重复下载。

使用 NVIDIA GPU Docker（需要 NVIDIA 驱动以及 Docker 的 GPU 支持）：

```powershell
docker compose -f docker-compose.yml -f docker-compose.gpu.yml up --build
```

## 4. 配置中文翻译服务

纯中文视频不需要配置翻译服务。处理英语、日语、韩语等外语视频时，必须在 `.env` 中配置以下任意一种服务。

### 4.1 OpenAI 或其他兼容接口

```dotenv
TRANSLATION_PROVIDER=openai_compatible
TRANSLATION_BASE_URL=https://api.openai.com/v1
TRANSLATION_API_KEY=你的_API_Key
TRANSLATION_MODEL=gpt-4.1-mini
```

兼容 OpenAI `/chat/completions` 协议的其他服务，只需替换 `TRANSLATION_BASE_URL`、`TRANSLATION_API_KEY` 和模型名。

### 4.2 本地 Ollama

先安装 Ollama 并下载一个擅长中文翻译的模型，例如：

```powershell
ollama pull qwen3:8b
```

本机直接运行言幕时：

```dotenv
TRANSLATION_PROVIDER=openai_compatible
TRANSLATION_BASE_URL=http://localhost:11434/v1
TRANSLATION_API_KEY=ollama
TRANSLATION_MODEL=qwen3:8b
```

通过 Docker 运行言幕时，把地址改为：

```dotenv
TRANSLATION_BASE_URL=http://host.docker.internal:11434/v1
```

### 4.3 LibreTranslate

已有 LibreTranslate 服务时：

```dotenv
TRANSLATION_PROVIDER=libretranslate
TRANSLATION_BASE_URL=http://localhost:5000
TRANSLATION_API_KEY=
```

每次修改 `.env` 后，都要重启言幕服务。

## 5. 网页操作

1. 将视频拖入“添加视频”区域，或点击该区域选择文件。
2. 选择识别精度。第一次使用某个模型时，系统会自动下载模型。
3. 如需直接播放的成片，打开“压制到视频”；只需要可编辑字幕时无需打开。
4. 点击“开始生成字幕”。视频先上传到本机服务，再进入处理队列。
5. 在右侧查看语音识别、翻译、字幕生成和视频压制进度。
6. 任务完成后点击任务卡片，预览视频并下载字幕或带字幕视频。

支持的视频扩展名：`MP4`、`MOV`、`MKV`、`AVI`、`WebM`、`M4V`、`MPEG`、`MPG`、`TS`。

## 6. 模型选择

| 模型 | 速度 | 准确度 | 适用场景 |
| --- | --- | --- | --- |
| Tiny | 最快 | 基础 | 测试流程、清晰短视频 |
| Base | 很快 | 一般 | 普通短视频 |
| Small | 均衡 | 良好 | 默认推荐，适合多数视频 |
| Medium | 较慢 | 很好 | 口音、嘈杂环境、专业内容 |
| Large v3 | 最慢 | 最佳 | 对准确度要求高、硬件较强 |

CPU 电脑建议从 `Small` 开始。`Large v3` 需要更多内存与磁盘空间；长视频处理时间可能接近或超过视频时长。

高级用户可在 `.env` 中设置：

```dotenv
WHISPER_MODEL=small
WHISPER_DEVICE=auto
WHISPER_COMPUTE_TYPE=auto
MAX_CONCURRENT_JOBS=1
MAX_UPLOAD_MB=4096
```

网页选择的模型会覆盖 `WHISPER_MODEL` 默认值。普通电脑不建议提高 `MAX_CONCURRENT_JOBS`。

## 7. GPU 加速

言幕包含两类彼此独立的 GPU 加速：

- **语音识别**：NVIDIA CUDA 加速 faster-whisper，这是整个处理流程中收益最大的部分；
- **字幕视频压制**：自动检测 NVIDIA NVENC、Intel Quick Sync、AMD AMF 或 Apple VideoToolbox 硬件编码器。

网页“硬件加速”建议保持“自动检测”。检测到 CUDA 时优先使用 GPU FP16 识别；CUDA 运行库缺失、驱动不兼容或显存不足时自动使用 CPU INT8 重试。硬件视频编码器虽然出现在 FFmpeg 编译列表中，但如果驱动实际不可用，任务会自动回退到 CPU x264。

### 7.1 NVIDIA CUDA 环境

当前版 faster-whisper / CTranslate2 的 GPU 运行环境要求 NVIDIA 驱动、CUDA 12 的 cuBLAS 和 CUDA 12 的 cuDNN 9。具体版本要求以 [faster-whisper 官方说明](https://github.com/SYSTRAN/faster-whisper#gpu)为准。

先确认驱动能识别显卡：

```powershell
nvidia-smi
```

启动言幕后，页面右上角显示“CUDA”即表示 CTranslate2 已检测到显卡。也可以访问 <http://localhost:8000/api/health> 查看 `cuda_available` 和 `cuda_device_count`。

强制使用 CUDA 可在 `.env` 中设置：

```dotenv
ACCELERATION_MODE=cuda
WHISPER_DEVICE=auto
WHISPER_COMPUTE_TYPE=auto
# Windows：也可直接指定 CUDA DLL 文件所在目录
CUDA_DLL_DIRECTORY=C:\path\to\cuda12-libs
```

如果显存不足，先改用 `Small` 模型；仍然不足时选择“仅使用 CPU”。

### 7.2 检查视频硬件编码器

```powershell
ffmpeg -hide_banner -encoders | findstr /i "h264_nvenc h264_qsv h264_amf"
```

可在 `.env` 强制指定编码器：

```dotenv
# NVIDIA
GPU_VIDEO_ENCODER=h264_nvenc
# Intel 可改为 h264_qsv；AMD 可改为 h264_amf
```

只有勾选“压制到视频”时才会使用视频编码器。ASS 字幕渲染仍需要 CPU，因此硬件编码加速幅度取决于视频分辨率和电脑配置。FFmpeg 官方也说明：编译时列出的硬件加速组件仍依赖合适的运行时硬件和驱动。

## 8. 输出文件

每个任务位于：

```text
data/jobs/<任务 ID>/
├── job.json          # 任务状态和元数据
├── source.mp4        # 上传的源视频，扩展名按原文件保留
├── 简体中文字幕.srt  # 中文视频繁转简后的字幕
├── 双语字幕.srt      # 外语视频的双语字幕
├── 字幕样式.ass      # 用于硬字幕压制的样式文件
├── 带字幕_视频名.mp4 # 勾选压制时生成
└── processing.log   # 失败任务的详细错误日志
```

SRT 文件使用 UTF-8 BOM 编码，兼容常见播放器和剪辑软件。双语字幕第一行为外文原文，第二行为简体中文；硬字幕画面同样保持原文在上、中文在下。

## 9. 常见问题

### 页面提示“需要安装 FFmpeg”

项目会在系统 PATH 中找不到 FFmpeg 时使用依赖自带版本。若仍然出现提示，可安装系统 FFmpeg，或在 `.env` 中设置可执行文件的绝对路径：

```dotenv
FFMPEG_BINARY=C:\ffmpeg\bin\ffmpeg.exe
FFPROBE_BINARY=C:\ffmpeg\bin\ffprobe.exe
```

### 首次任务长时间停留在“识别人声与语种”

首次使用会从网络下载 Whisper 模型。检查网络和磁盘空间，完成后后续任务会复用本地模型。

### 页面显示 CUDA 未检测到

先运行 `nvidia-smi`。如果该命令失败，请更新 NVIDIA 驱动；如果该命令成功但页面仍显示未检测，请安装 CUDA 12 的 cuBLAS 与 cuDNN 9，然后重启言幕。也可以先使用“仅 CPU”完成任务。

### FFmpeg 能看到 NVENC，但任务显示回退到 CPU

FFmpeg 包含 `h264_nvenc` 不代表当前驱动和显卡一定可用。更新 NVIDIA 驱动并确认显卡支持 NVENC；言幕会在硬件编码调用失败时删除不完整文件，然后自动使用 x264 重试。

### 外语视频提示“需要翻译服务”

检查 `.env` 中的 `TRANSLATION_API_KEY`、接口地址和模型名称，并在修改后重启服务。本地 Ollama 还需确认 `ollama list` 中已有对应模型。

### 没有识别到清晰语音

确认视频不是静音、语音音量足够且没有严重背景噪声。可尝试 `Medium` 或 `Large v3` 模型。

### 字幕压制失败但 SRT 已生成

打开任务目录中的 `processing.log` 检查 FFmpeg 错误。常见原因是 FFmpeg 缺少 `libass` 或 `libx264`；建议安装完整构建版 FFmpeg，或使用 Docker 版本。

### 如何修改上传大小限制

编辑 `.env`：

```dotenv
MAX_UPLOAD_MB=8192
```

修改后重启服务。还要确保磁盘空间足够，压制视频时可能同时保存源文件和输出文件。

## 10. API 调用

启动后可访问 <http://localhost:8000/docs> 查看交互式 API 文档。

上传视频示例：

```powershell
curl.exe -X POST http://localhost:8000/api/jobs `
  -F "file=@D:\Videos\sample.mp4" `
  -F "model_size=small" `
  -F "acceleration=auto" `
  -F "burn_subtitles=false"
```

查询任务：

```powershell
curl.exe http://localhost:8000/api/jobs/任务ID
```

下载字幕：

```powershell
curl.exe -L http://localhost:8000/api/jobs/任务ID/files/subtitle -o subtitle.srt
```

## 11. 数据与隐私

视频、字幕和任务记录默认只保存在运行言幕的电脑或服务器上。使用云端翻译接口处理外语视频时，识别出的字幕文本会发送给所配置的翻译服务，但不会上传原始视频。若要求全部内容留在本机，请使用本地 Ollama 或自建 LibreTranslate。
